<h1><%=@pat.name %></h1>

<h2><%=@pat.age%> years old</h2>

<%@pat.appointments.each do |a|%>
  <p>Time: <%=a.appointment_datetime.strftime("%B %d, %Y at %H:%M")%></p>
  <p>Doctor: <%=link_to a.doctor.name, doctor_path(a.doctor)%></p>
  <hr>
<%end%>