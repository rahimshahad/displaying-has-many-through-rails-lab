<h1><% @doc.name %></h1>

<%=@doc.department%>
<%@doc.appointments.each do |a|%>
  <p><%=a.appointment_datetime.strftime("%B %d, %Y at %H:%M")%></p>
  <p><%=link_to a.patient.name, patient_path(a.patient)%></p>
  <hr>
<%end%>